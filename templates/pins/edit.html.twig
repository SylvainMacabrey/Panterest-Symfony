{% extends 'base.html.twig' %}

{% block title %}Panterest - Modifier l'épingle{% endblock %}

{% block body %}

    <div class="row">

        <div class="col-md-6 mx-auto">

            <h2> Modifier l'épingle {{ pin.title }} </h2>

            {{ include ('pins/_form.html.twig', {button: 'Modifier'}) }}

            <a class="btn btn-secondary btn-block" style="margin-top: 20px" href="{{ path('pins.show', { id: pin.id }) }}"> Retour à l'épingle </a>

        </div>

    </div>

{% endblock %}